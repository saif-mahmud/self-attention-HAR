dirs:
  saved_models: ./saved_model

training:
  loss: 'categorical_crossentropy'
  optimizer: 'adam'
  metrics: [ 'accuracy' ]

pamap2:
  epochs: 50
  batch_size: 128
  d_model: 128
  dropout: 0.2
  n_head: 4

skoda:
  epochs: 50
  batch_size: 256
  d_model: 128
  dropout: 0.2
  n_head: 4

opp:
  epochs: 50
  batch_size: 256
  d_model: 128
  dropout: 0.2
  n_head: 4

uschad:
  epochs: 50
  batch_size: 256
  d_model: 128
  dropout: 0.2
  n_head: 4
